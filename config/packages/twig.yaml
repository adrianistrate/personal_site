twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    form_themes:
        - '@SonataCore/Form/datepicker.html.twig'
    globals:
        image_manipulation: '@App\Service\ImageManipulation'
        ga_code: '%env(GA_CODE)%'
        hj_code: '%env(HJ_CODE)%'